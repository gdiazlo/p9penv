#!/usr/bin/env bash

if [ -z "$1" ]; then
	echo 12 > /tmp/.acme_font_size
	echo Setting up 12 as font size
	exit 0
fi

s="$1"
isNumber='^[0-9]+$'

if [[ ${s} =~ ${isNumber} ]]; then
	echo "${s}" > /tmp/.acme_font_size
else
	echo Usage: sfont size
	exit -1
fi

